#include "main.h"
/**
 * _islower - print 1 is lowercase otherwise 0
 * @c: the carracter
 *
 * Return: 1 other lowercase otherwise 0
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
