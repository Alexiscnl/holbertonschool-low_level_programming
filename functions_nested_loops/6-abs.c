#include "main.h"
/**
 * _abs - print number negativ or positiv
 *@n: the caracter
 *
 * Return: positiv if n < 0, otherwise return negativ
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
