#include "main.h"
/**
 * _isupper - dyplay 0 if lower otherwise dysplay 1
 *@c: A: , a:
 *
 * Return: 0 if uppercase otherwise return 1
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
